@* ReSharper disable once CheckNamespace *@
@namespace Andromeda
@inherits GslComponentBase

<MudPaper Elevation="2">
    <MudToolBar>
        
        @if (!string.IsNullOrWhiteSpace(Page.BackHref))
        {
            <MudIconButton Href="@Page.BackHref" Icon="@Icons.Material.Outlined.ChevronLeft" Color="Color.Inherit" Class="tw:!mr-2"/>
        }
        
        <MudSpacer></MudSpacer>

        @ChildContent
        
    </MudToolBar>
</MudPaper>